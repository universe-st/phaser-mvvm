# AGENTS.md

面向 AI 编码代理与自动化协作者的仓库操作手册。**人读的入门文档是 [`README.md`](./README.md) 与 [`CONTRIBUTING.md`](./CONTRIBUTING.md)；架构事实来源是 [`docs/PLAN.md`](./docs/PLAN.md)。** 本文件只写「怎么在这个仓库里干活」，不复制 PLAN 的规格表。

> 30 秒版：pnpm workspace 单仓 → 改代码 → `pnpm --filter <包名> run typecheck` + `run test` → `pnpm format`。**`core`/`layout` 必须零 Phaser 依赖**（`phaser`/`widgets` 可以 import，见 §4.1）；`docs/PLAN.md` 与 `packages/layout/test/golden/` 不要格式化。

---

## 1. 项目是什么

**phaser-mvvm** —— 基于 **Phaser 4**（`phaser@4.2.1`）的 MVVM UI 框架：响应式数据 + 声明式视图 + 两阶段自动布局 + 基础控件库。**不 fork Phaser**，只用公开 API 与插件/工厂注册；游戏逻辑照常使用原生 Phaser。

## 2. 环境与命令

- **Node 24**（CI 固定 24；本机验收环境 `v24.18.1`）、**pnpm 10.34.5**（根 `packageManager` 固定）。
- 依赖已安装时**不要**重复 `pnpm install`；只有需要同步 lockfile 时才安装，并提交更新后的 `pnpm-lock.yaml`。
- 示例 dev server 端口写死 **5173 + `strictPort: true`**：被占用会直接报错而不是换端口。若需另起服务，用后台任务并核实端口。

| 命令                                                          | 用途                                                                                                                                                                                                                             |
| ------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm typecheck`                                              | `pnpm -r run typecheck`（逐包 `tsc -p tsconfig.json`）                                                                                                                                                                           |
| `pnpm test`                                                   | `pnpm -r run test`（逐包 vitest）                                                                                                                                                                                                |
| `pnpm build`                                                  | `pnpm -r run build`（tsup：ESM + CJS + d.ts）                                                                                                                                                                                    |
| `pnpm format` / `pnpm format:check`                           | Prettier 写 / 校验（CI 门禁是 `prettier --check .`）                                                                                                                                                                             |
| `pnpm dev`                                                    | 示例 dev server → http://localhost:5173（场景：`#/showcase` 工厂 API 验收页、`#/compose` DSL 验收页、`#/m0` 等）                                                                                                                 |
| `pnpm build:examples` / `pnpm preview`                        | 构建示例 / 预览产物（4173，`strictPort`）                                                                                                                                                                                        |
| `pnpm --filter @phaser-mvvm/<pkg> run typecheck\|test\|build` | **并行开发期优先用这个**，只验证自己负责的包                                                                                                                                                                                     |
| `pnpm docs:check`                                             | 指南门禁四关：片段标识符必须真的导出 + **idiom**（不许写 `style: { fontSize … }`）+ **vocabulary**（DSL 导出必须有 demo 与指南提名）+ **option keys**（指南选项表里每个键都必须被它文档化的那个包接受），见 §6/§8.52/§8.55/§8.65 |
| `node scripts/visual-check.mjs`                               | 几何 + 像素验收（见 §6），需要本机 Chrome/Chromium                                                                                                                                                                               |
| `UPDATE_GOLDEN=1 pnpm --filter @phaser-mvvm/layout run test`  | 重新生成布局黄金快照                                                                                                                                                                                                             |

包名：`@phaser-mvvm/core`、`@phaser-mvvm/layout`、`@phaser-mvvm/phaser`、`@phaser-mvvm/widgets`，示例为 `@phaser-mvvm/examples`（私有）。

## 3. 目录与依赖方向

```
apps/examples      Vite 示例与验收场（场景注册在 src/main.ts 的 SCENES）
packages/core      响应式内核 / 调度器 / 绑定上下文 / 表达式编译 —— 零 Phaser 依赖
packages/layout    渲染无关的两阶段布局引擎（measure/arrange）—— 零 Phaser 依赖
packages/phaser    Phaser 4 适配层：Widget/UIRoot/MVVMPlugin/输入·焦点·导航/主题/绑定（与 widgets 一样 import phaser）
packages/widgets   控件库：Label/Panel/Button/Image/Spacer/Divider/TextField/TextArea/ScrollView/Repeat
                   子路径 `@phaser-mvvm/widgets/compose` = Compose 风格 DSL（推荐写视图的方式）
docs/              PLAN.md（唯一事实来源）、guide/（教程式使用指南：控件/布局/用法/DSL）、adr/（ADR-0001…0008）、ACCEPTANCE-*.md（验收记录）、DEFECT-BACKLOG.md（审计缺陷登记簿）
scripts/           visual-check.mjs（CDP 无头 Chrome 几何+像素验收）、png-sample.py（Pillow 采样）
```

依赖方向严格单向、禁止反向与环：`apps → widgets → phaser → { core, layout }`；`core` 与 `layout` 互不依赖（见 [ADR-0001](./docs/adr/0001-package-layout.md)、[ADR-0003](./docs/adr/0003-layout-is-renderer-agnostic.md)）。

## 4. 硬约束（改动前先读）

1. **Phaser 只出现在适配层与控件层**：`core`/`layout` **零 Phaser 依赖**（连类型都不能引）；`packages/phaser` 与 `packages/widgets` 可以把 `phaser` 当 **peer dependency 直接 import**（每个控件都在 `new Phaser.GameObjects.…`，这是既成事实，第 98 轮把这句话改对了——原文写的是"widgets 只通过 `@phaser-mvvm/phaser` 间接使用"，与代码不符）。`apps/` 只依赖 `widgets`。构建时 `phaser` 一律 `--external`。
   例外与延伸：`packages/phaser/src/uiscope.ts`（DSL 作用域机制）是**纯逻辑**，只允许 `import type` Phaser，必须保持可在 Node 单测；`packages/widgets` 的**纯逻辑模块**（`text-edit`/`text-truncate`/`scroll-plan`/`branch-plan`/`keyboard-plan`…）同样必须零 Phaser，因为它们要能在 Node 里单测。
2. **布局算法不依赖渲染**：文本度量通过注入的 `Measurer` 接口获得；测试用等宽假测量器，保证黄金快照确定。
3. **裁剪用 Mask filter**（`FilterList#addMask`），不要用 v3 的 `setMask(graphics)` 思路——`GeometryMask` 在 Phaser 4 仅 Canvas 可用（[ADR-0007](./docs/adr/0007-phaser4-webgl-constraints.md)）。
4. **响应式副作用必须归入 `EffectScope`**：`destroy()` 里 `scope.stop()` + 注销输入 + 归还对象池；「场景创建→销毁 100 次后计数归零」是硬门禁（[ADR-0008](./docs/adr/0008-reactivity-and-scheduler.md)）。
5. **UI 刷新默认帧对齐**（`flush: 'frame'`）：不要用 `sync` 绕过批量刷新。
6. **不使用 `eval` / `new Function`**：路径表达式编译为 getter/setter 闭包，保证 CSP 可用。
7. **`docs/PLAN.md` 是唯一事实来源**：实现细节与它冲突时，先改 PLAN（或新增 ADR），再改代码。
8. **以下改动必须新增一篇 ADR**（新编号，**不得修改历史 ADR 的决策正文**）：新增发布包、改变包间依赖方向、引入新的运行时依赖（或在 `core`/`layout` 中引入任何依赖）、改变布局模型/响应式·调度语义/裁剪方案等已冻结决策。模板与流程见 [`docs/adr/README.md`](./docs/adr/README.md)。

## 5. 代码约定

| 约定     | 要求                                                                                                                                                                                                                             |
| -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 类型     | 继承 `tsconfig.base.json`：`strict`、`noUncheckedIndexedAccess`、`noImplicitOverride`、`verbatimModuleSyntax`、`noUnusedLocals/Parameters`、`noEmit`。禁用 `any` 与 `@ts-ignore`，用 `unknown` + 收窄。                          |
| 导入     | 相对导入**不带扩展名**（`./reactivity/ref`）；跨包用包名（`@phaser-mvvm/core`），不要用相对路径穿透 `packages/`。                                                                                                                |
| 格式     | Prettier：单引号、`printWidth: 100`、`trailingComma: all`、分号、`arrowParens: always`。                                                                                                                                         |
| 注释语言 | 公开 API 的 JSDoc 用**英文**；解释性的设计注释可用中文。既要「为什么」而不是复述代码。                                                                                                                                           |
| 提交信息 | Conventional Commits + **英文祈使句**简述，例：`feat(widgets): ScrollView with WebGL clipping and gestures`、`fix(layout): keep the arrange pass descending to a dirty boundary`、`docs: M7 acceptance record`。文档正文用中文。 |

## 6. 测试与验收

- **单测**：vitest，测试放 `packages/<pkg>/test/*.test.ts`；仓库**没有** vitest 配置文件，用默认 include。`core`/`layout`/`phaser`/`widgets` 的测试都必须在纯 Node 下跑通（不启动渲染器）。
- `packages/phaser`、`packages/widgets` 的 `test` 脚本带 `--passWithNoTests`——不要删掉这个 flag，也**不要**为了「有测试」写空断言。
- **黄金快照**：`packages/layout/test/golden/*.json`，零漂移是门禁；确认是有意变更后再用 `UPDATE_GOLDEN=1 pnpm --filter @phaser-mvvm/layout run test` 重生成，并在说明里解释每一处差异。该目录在 `.prettierignore` 中。
- **几何 + 像素验收**：`node scripts/visual-check.mjs`。脚本用**单个**无头 Chrome（CDP + 固定视口 1280×720）逐场景：读页面 `#status`（场景把引擎实际分配的 rect 写进 DOM）→ 截图 → 按 canvas 偏移换算后采样中心像素并与期望填充色比对。常用参数：`--no-build`、`--size 1024x768`、`--port`、`--out .tmp/visual-check`；需要 `CHROME_PATH` 或本机 Chrome/Chromium，**仅 macOS/Linux 可用**。
  - 要校验的场景在脚本里**硬编码**（当前 `m0`、`probe`、`stack`、`hud`、`modal`、`uiscene`、`a11y`、`compose`、`options`、`keyboard`、`showcase`；`options` 的四个采样点是双轴口的滚动条 A/B，见 `ACCEPTANCE-options.md` §8.4）——新增场景若需像素断言，必须同时加进 `PIXEL_EXPECTATIONS` 与该场景列表。
  - **`AX_EXPECTATIONS`（可访问性树断言）**：与像素无关，只声明期望的控件节点（角色/名字/属性），而语义是"**控制节点恰好这么多、每条恰好一次**"——所以新增一个控件必须补一行（第 101 轮给 `#/keyboard` 加「对话框里输入」时就是这条门禁提醒的），而**没列出来的控件必须真的不在树里**。`modal` 与 `pages` 两条正是后者的用法：`modal` 的 scene setup 开着 `confirm` 对话框、`pages` 的 setup 推入详情页，两条都只列"当前活着的那一层"的控件 —— 被盖住的页面若还在树里（V72/V73）会当场红。加进这一表的场景必须有 `#a11y` 场景那样的探针（`window.a11y.nodes()` 等），`AX_TAB_SCENES` 里的场景还会按一次真实 `Tab` 并断言"恰好一个控制节点带 focus"。
  - 需要先摆好状态的场景用 `SCENE_SETUP`（页内表达式，截图前执行；**返回 Promise 会被 await**，所以「等两帧再报告几何」这类准备可以写成一个 async 方法，`showcase` 就是这么做的）：`hud` 用它把相机滚到 (260,140)，于是「HUD 钉住不动 + 世界真的滚动」两件事都被像素断言（`score` 按钮在布局坐标上仍是 `#2f6feb`，而世界坐标 (960,520) 的瓦片色 `#161b22` 只有在相机滚动后才成立）。
  - 通用检查「画布清屏色」默认在画布内 4px 处取样（假设左上角是空的）；`#/hud` 的顶栏盖在那里，所以它用 `CANVAS_CLEAR_AT` 把取样点挪到两块瓦片之间的 2px 缝上（画布相对坐标 (340,60)），那里能看见相机背景色。
  - 被其它控件遮挡的采样点用 `{ rgb, fx, fy }` 指定相对采样位置。
  - **每个场景跑两遍**：暗色半场用 `PIXEL_EXPECTATIONS`，亮色半场（`setTheme('light')` 后重新截图）用同一批采样点比 `LIGHT_EXPECTATIONS`。亮色里既有"令牌必须变"的条目（`confirm.ok` `#f85149→#cf222e`），也有"字面量必须不变"的条目（`Rect` 的色块、世界瓦片、贴图），所以"全都变了"不能冒充"重绘正确"；新增控件若要进这条矩阵，同时写两处。亮色半场复用暗色半场的 `#status` 几何——主题切换不该移动任何东西。
  - 少数场景的**角落本来就不是清屏色**：`CANVAS_CLEAR_SKIP` 里的场景跳过通用 `canvas.clear` 检查，改由它自己的采样承担（`modal` 就是——模态遮罩按设计盖满整块画布，角落是「主题背景 × 半透明黑」的混合值，属于 GPU 取整问题）。跳过时**必须**在该场景补更具体的断言，不允许只是删掉检查。
  - 脚本起的 **preview 服务是 detached 进程组**（`pnpm exec vite preview` 被 `SIGTERM` 时不会带走真正的 vite 进程，早期版本因此每次运行都漏一个僵尸服务器占住端口，下轮就会报 `timed out waiting for vite preview`，见 DEFECT-BACKLOG V31）——改动脚本的收尾逻辑时请保持 `killGroup()`。
  - **`--no-build` 用的是 `dist/` 里上一次构建的包**：源码改完（尤其是把阳性对照还原）之后再用 `--no-build` 复跑，读到的还是旧包的结果——"改回去也没修好"通常是这个原因。改过源码就重新构建（或别加这个参数）。
  - 脚本启动 Chrome 时**不传** `--user-data-dir`，因此依赖默认 profile 目录可写（`~/Library/Application Support/Google/Chrome`）。在受限/沙箱环境里 Chrome 会直接崩溃（Crashpad 写盘被拒），脚本报 `timed out waiting for chrome devtools endpoint` —— 这是环境限制而非脚本缺陷；此时请在汇报里说明「像素验收未运行」，并至少断言 `#status` 里的几何。
- **指南片段门禁**：`pnpm docs:check`（`scripts/check-doc-snippets.mjs`）做两件事：① 把 `docs/guide/*.md` 的 `ts` 代码块里**被调用**的标识符与四个包（含 `widgets/compose` 子路径）的公开导出对照，抓「改了 API 没改文档」与拼写错误；② **idiom 检查**：指南的 `ts` 片段与 `apps/examples/src/scenes/**` 里不许再出现 `style: { fontSize … }` 这种长写法（`Label`/`Text` 用 `size: 'lg'` 或 `size: 18`；要演示 `style.fontSize` 优先于 `size` 的那一行标 `idiom-ok`，理由见 §8.52）；③ **vocabulary 检查**：`widgets/compose` 的每个运行时导出都必须在示例里被用到、并在指南里被提名（理由见 §8.55：没人用过的别名 `Surface` 就是这样被删掉的）。它是语法层面的检查（先剥掉注释、字符串与 `{{ … }}` 模板占位，跳过片段内自己声明的名字），不是编译器；新增/重命名导出后请跑一次。 ④ **option keys 检查**（第 104 轮，`scripts/check-doc-options.mjs`）：解析指南里每张键表（表头首格是「选项」或「字段」），按**标题栈**把它归属到文档化的那个选项包（widget / `LayoutParams` / `ModalOptions` / `PageOptions` / `TransitionOptions`），再拿该包真正接受的键核对 —— 补的是「文档承诺了、代码不接受」这条缝（`#/options` 是从**控件**键表反查出来的，抓不到它）；当前 195 个键全过，5 张表按设计跳过（散文表、主题令牌表、README 索引）。**加一张新的选项表时让标题自己说清归属**（写 `## 2. \`ScrollView\` 选项`，而不是裸的 `## 2. 选项`），新选项包要加进脚本的 `BAGS`。第 75 轮起**带类型实参的调用**（`routeParams<{ id: string }>(params)`）也会被扫描——此前这类调用整个漏检，往里写错名字照样报 ok。
- **指针落点走树有 Node 单测**：`packages/phaser/test/target-walk.test.ts` 覆盖 `resolveTargetInTree()`（最深优先、隐藏子树跳过、容器链偏移、每个候选自己的坐标空间、**裁剪容器把整棵子树移出命中**等 14 条）——这是 V1/V8/V9/V23 四次缺陷的所在，改动 `InputRouter` 的命中逻辑（含 `pointerInWidgetSpace`、`clipsPointer`）时先跑它，再用浏览器验收。
- **控件与布局全家福**：`#/showcase`（`apps/examples/src/scenes/showcase.ts`）是「每个控件、每个布局容器、每组布局参数」的验收页，十一个分区（`text`/`buttons`/`inputs`/`decoration`/`box`/`grid`/`stack`/`params`/**`sizing`**/`repeat`/`focus`）。`window.showcase` 提供 `sections()`/`show(id)`/**`showAndReport(id, frames?)`（`show()` + 等两帧 + 把该分区的门禁控件写进 `#status`，返回 Promise —— `reportWidget()` 在构建趟里读到的是 `0×0`，见 §8.48）**/`showAll()`/`state()`/`geometry()`/`controls()`/**`controlStates()`**（每个控件的 `state`/`point`/`interactive`/**`inBand`**/`a11y`）/**`rects(names?)`（分区里每个具名控件的 `appliedRect` + 页面坐标与尺寸，用来把布局参数断言成数字）**/**`truncation()`**（`Label · truncation` 卡两张标签的**画出来**的行数 / 是否掉行 / 结尾是否 `…` / **实际生效的宽度**，第 98 轮 V66 的常驻读数）/`counts()`/`churnSections(n)`/`paramsBlocks()`/`scrollStage(y)`/`stageRect()`；`#demo-state` 逐帧发布 `section`/`widgets`/`clicks`/`toggled`/`field`/`email`/`theme`/`repeat.*`/`scroll.offset` 与每个被跟踪控件的 `pt.<key>` **和 `st.<key>`**（视觉状态）。**三条读数纪律**：① `pt.*` 只说明控件摆在哪，**不说明它是否被舞台裁掉**——`params` 分区高 1513px 而舞台 672px，`pt.params.visibility` 会在画布之外（先看 `inBand`，必要时 `scrollStage()`）；② 容器的 `st.*` 只在指针落在**容器自己**上时才变，落在子控件上时路由会指向子控件（`repeat.chips` 下面就是一个 chip，这不是缺陷）；③ 切换分区会销毁并重建子树，而**路由器的目标集合下一帧才重收集**——比较计数前先 `mvvm.refreshInteraction()`（`churnSections()` 就是这么做的）。矩阵见 [`ACCEPTANCE-showcase.md`](./docs/ACCEPTANCE-showcase.md)（第 89 轮的第十一个分区 `sizing` 覆盖 `shrink`/`basis`/`minHeight`/`maxHeight`，`grid` 分区补了 `gridColumn`/`gridRow`/`gridRowSpan`；该分区同时是 `scripts/visual-check.mjs` 的像素门禁：`SCENE_SETUP.showcase` 用 `showAndReport("sizing")`，四个采样点与正对照见 §7.5）。
- **交互状态矩阵**：`#/states`（`apps/examples/src/scenes/states.ts`）把每个有状态的控件摆成一行，并把 `visualState` 逐帧写进 `#demo-state` 的 `st.<name>`，坐标写进 `pt.<name>`。验收方式：用真实 `mouse.move`/`down`/`up`、`keyboard.press` 驱动悬停/按下/聚焦/校验，断言 `st.*` 的迁移（滑杆探针为 `slider.volume`/`slider.stepped`/`slider.disabled`，见 [`ACCEPTANCE-slider.md`](./docs/ACCEPTANCE-slider.md)）；**`window.states.events()`/`clearEvents()` 把 `WIDGET_EVENTS` 也变成读数**（每个探针订阅 `widget:activate`/`widget:state`，逐帧发布 `events.*`）——真鼠标/真键盘/假手柄/真触摸四条路径的 `ActivationSource` 与状态流矩阵见 [`ACCEPTANCE-states.md`](./docs/ACCEPTANCE-states.md) §7；在视口外的探针先用 `window.states.reveal(name)` 滚进视野（否则坐标在画布外，指针事件落不到控件上）。
- **相机钉住的 HUD**：`#/hud`（`apps/examples/src/scenes/hud.ts`）是 ADR-0009 的常驻验收场：2400×1600 的滚动世界 + `page.setScrollFactor(0)` 钉住的 HUD 页。`window.hud` 提供 `scroll(x,y)`/`auto(on)`/`spawn()`/`hitTest(name,x,y)`/`states()`/`states().<name>`；`#demo-state` 里的 `pt.<name>` 是页面坐标，`world.clicks` 是「点击穿透到游戏世界」的计数：页面的 `blockPointer: false` 让世界区域的点击照常到达游戏对象，而顶栏/底栏面板体与按钮会吞掉它（第 43 轮修复的 V9，矩阵见 `docs/ACCEPTANCE-hud.md` §5）。断言方式：先 `window.hud.scroll(260,140)` 再点 `pt.score`，读数应为 `clicks=1`、`focus=hud.scoreButton`；`spawn()` 后新增的按钮同样可点（`late.clicks=1`）。
- **表单状态机**：`#/form`（`apps/examples/src/scenes/form.ts`）是文本框/表单的综合验收页，暴露 `window.form`（`values()`/`errors()`/`states()`/`focus()`/`submitted()`/`submits()`/`canvas()`/**`setCanvas(key, value)`**/**`rect(key)`（实时页面矩形，拖动起止点必须同帧读，§8.47）**）与逐帧 `st.name`/`st.email`/`st.notes`/`st.submit`、`name.length`、`notes.breaks`、`email.valid`、`hint`、`submitted`（载荷是 `submitted#<序号> <来源>:<值>`，来源 = `name`/`email`/`notes`/`button`/`canvas`/`canvasArea`）、以及纯 Canvas 三个字段的 `canvas.*`/`canvasArea.*`/`canvasRo.*`（`value` 长度 / `caret` / `selection` / `bridged` / **`scroll` = `x,y`**，后者来自新公开的 `TextInputBase#scrollLeft`/`#scrollTop`）。**第 93 轮起该路径支持拖动选择、双击选词与三击选行**，**第 97 轮起拖动到框外会逐帧继续滚动**（`top`/`caret`/`selection` 在指针停住时仍然变化——这是"按住不动"唯一的可读数；拖动归属协议见 §8.53、点击计数窗口见 §8.54、逐帧推进与阳性对照见 §8.57 与 [`ACCEPTANCE-form.md`](./docs/ACCEPTANCE-form.md) §5.7；`#/options` 的 `drag to select (dom: false)` 卡是归属矩阵，多行那一路在 `#/form` 的 `canvasArea` 上验）。**三个字段是第 88 轮加的「纯 Canvas 路径」验收位**：`canvas`（单行）、`canvasArea`（多行、软换行）、`canvasRo`（`readOnly: true`）——`dom: false` 此前一个 demo 都没有，而 V17/V18/V59 三个输入缺陷全出自这条路径。验收矩阵（校验失败→修正、`maxLength`、`Enter` 提交、`Ctrl+Enter` 提交、触摸点击后输入、纯 Canvas 编辑/光标/选区/剪贴板/只读）见 [`ACCEPTANCE-form.md`](./docs/ACCEPTANCE-form.md)。**取点击坐标一律读实时 `rect`**（`window.game.scene.getScene('form').mvvm.input.widgets`），别用创建时采样一次的 `pt.*`/`#status`：本页加字段后面板 466px → 514px、整块上移 24px，旧快照会把人点到空白处（§8.47）。
- **绑定层状态机**：`#/bindings`（`apps/examples/src/scenes/bindings.ts`）逐帧发布 `pt.*` 与 `st.*`（`add`/`remove`/`details`/`busy`/`replace`）。验收矩阵（派生值随点击重算、`bindEnabled` 在两个边界自禁用、`bindVisible` 折叠后面板行位移、`bindCommand` 的 `canExecute` 期间禁用、`Replace view` 后绑定重挂）见 [`ACCEPTANCE-bindings.md`](./docs/ACCEPTANCE-bindings.md)。
- **控件画廊**：`#/gallery`（`apps/examples/src/scenes/gallery.ts`）是"每个控件、每种状态"的工厂 API 页：九个按钮全部命名并逐帧发布 `pt.<name>`/`st.<name>`，另发布 `focusables`（焦点集合的名字列表）。验收矩阵（禁用按钮拒绝悬停/点击且不在焦点集合、loading 拒绝激活但可聚焦、开关鼠标与键盘都能翻转、Tab 顺序与环绕）见 [`ACCEPTANCE-gallery.md`](./docs/ACCEPTANCE-gallery.md)。
- **模态对话框**（**它同时是无障碍作用域的门禁**：`visual-check` 的 `AX_EXPECTATIONS.modal` 只列对话框自己的控件，而 AX 语义是"恰好这么多"，所以被盖住的页面若还留在无障碍树里会失败 —— V72，见 §8.62）：`#/modal`（`apps/examples/src/scenes/modal.ts`）是 `this.mvvm.modal` 的常驻验收场：五种对话框（确认 / 带输入框 / 不可关闭 / 无遮罩 / 嵌套两层）+ 一个 4000×4000 的「世界」（UI 之下的游戏对象）与两个页面按钮。`#demo-state` 逐帧发布 `depth`/`top`/`focus`/`focusables`/`page.clicks`/`world.clicks`/`closes`/`pt.*`/`st.*`/`counts.*`；`window.modal` 提供 `open(kind)`/`point(kind,name)`/`close`/`closeAll`/`state()`/`reasons()`/`counts()`/`churn(n)`/`scrim()`；`#demo-state` 另外逐帧发布 `scrim=` 与 `scrim.overlay=`（**两者必须永远相等**，这是"遮罩跟随主题"的门禁，V38）。**验收要点是 A/B**：同一个点击在「没有对话框」时必须打到页面按钮或世界（`page.clicks`/`world.clicks` 增 1），在「有对话框」时必须两者都不增、只把对话框按 `backdrop` 关掉。矩阵与两个顺带修掉的输入缺陷（V17/V18）见 [`ACCEPTANCE-modal.md`](./docs/ACCEPTANCE-modal.md)。**动效**（第 74 轮）：`#demo-state` 另外逐帧发布 `motion.pending`/`motion.enter`/`motion.exit`/`motion.reduced`/`motion.instant` 与 `motion.body`/`motion.scale`/`motion.scrim`（控件销毁后为 `gone`），`window.modal` 提供 `motion()`/`settle()`/`openAndSettle(kind)`/`instant(on)`/`transition(patch)`。**`churn(n)` 因此是 async 的**（每轮 `await settle()` 之后才采样——关闭后的图层要等出场动画结束才销毁），`counts.widgets` 也改成从 **UI 根**数（否则一个还在淡出的图层会被算成"干净"）。矩阵见 [`ACCEPTANCE-transition.md`](./docs/ACCEPTANCE-transition.md)（第 91 轮把 spec 的其余字段也量了：`easing` 的曲线形状、`delay`、自定义 `fromScale`/`toScale`、以及 `duration: 0` 的终态 —— T16–T19，并在同一轮修掉 V61：调用方按 duration 过滤目标，让 runner 的"零长度也应用终态"成了死代码）。
- **页面栈**（**它同时是无障碍层级门禁**：`visual-check` 的 `AX_EXPECTATIONS.pages` 配合 `SCENE_SETUP` 推入详情页，只列详情页自己的 3 个控件 —— 被盖住的列表页若还留在无障碍树里会失败，V73/§8.63）：`#/pages`（`apps/examples/src/scenes/pages.ts`）是 `this.mvvm.pages` 的常驻验收场：列表页（含一个可滚动列表、计数器与两个输入框）+ 详情页 + 第三层 + 从详情页打开的对话框。`window.pages` 提供 `open(i)`/`deeper()`/`dialog()`/`pop()`/`popToRoot()`/`point(name)`（**当前栈顶页**里某个控件的页面坐标，按需计算）/`scrollList(y)`/`listOffset()`/`state()`/`counts()`/`churn(n)`；`#demo-state` 逐帧发布 `depth`/`top`/`focus`/`focusables`/`clicks`/`field.length`/`list.offset`/`appBacks`/`resumes`/`disposes`/`motion.*`/`counts.*`/`pt.*`/`st.*`（`motion.pending` 是「还有没有在动」，`motion.routing` 是下面那页是否还是指针目标，`motion.departing`/`motion.departingAlpha` 是正在淡出的那一页——它已经不在 `handles` 里了）。**`churn(n)` 是 async 的**（每轮 `await settle()`：`pop()` 之后页面还要淡出 `exit` 毫秒才销毁），`counts.widgets` 也改成从 **UI 根**数（从页面栈数会漏掉幽灵页）。`PageOptions.transition` 可以逐页关掉或调整转场。**核心判据是「离开的那一页怎么了」**：推入后 `focusables` 只剩新页、被盖住的页不可点不可输入（DOM 桥失焦）、`pop()` 后计数/文本/滚动偏移逐字保留且焦点回到此前持有的控件；`Esc` 的顺序是「模态 → 页面 → `mvvm.onBack`」。矩阵与三个修掉的缺陷（V20/V21/V22）见 [`ACCEPTANCE-pages.md`](./docs/ACCEPTANCE-pages.md)。
- **路由表**：`#/router`（`apps/examples/src/scenes/router.ts`）是 `this.mvvm.router` 的常驻验收场：五条路由（`home`、`user/:id`、`user/:id/posts`、`settings`、`settings/advanced`）+ 每页一个输入框（证明「同一个 `user/:id` 的两次访问各有一份状态」）+ 「他的帖子」/「返回」/「设置」的按钮链。`window.routerDemo` 提供 `navigate(path, params?)`/`replace(path)`/`back()`/`popToRoot()`/**`popDirectly()`**（不经过路由弹页，用来验 `current` 跟着真实栈）/`unknown(path)`（返回错误消息而不抛）/`navigateWithLifecycle(path)`/`addRoute(path)`/`history()`/`keys()`/`tables()`/`point(name)`/`state()`/`counts()`/**`churn(n)`（async，每轮 `await settle()`）**/`settle()`/`openAndSettle(path)`。`#demo-state` 逐帧发布 `route.path`/`route.key`/`route.params`（`k:v|k:v`，不用 `=`）/`route.depth`/`route.history`/`route.names`/`route.focus`/`route.resumes`/`route.disposes`/`route.appBacks`/`route.visited`/`counts.*`/`pt.*`。**`pt.*` 只对可见页发布**（沿 `parentContainer` 判断是否在 `pages.top` 子树里，其余 `gone`）——被盖住的页是活的，探针却必须失效，否则会点到看不见的控件。矩阵与 `Router` 的两族单测见 [`ACCEPTANCE-router.md`](./docs/ACCEPTANCE-router.md)。
- **虚拟化列表**：`#/list`（`apps/examples/src/scenes/list.ts`）是 `Repeat` 虚拟化的常驻验收场（220 行、`itemExtent` 38、`overscan` 3）。`window.listDemo` 提供 `setTotal(n)`（把数据源换成 n 行，用来量 5000 项那条标准）与 `perf({frames, step})`（**在页面里用 rAF 驱动滚动并采样帧间隔**，返回 `{median, p95, max, fps, created, rendered, measureCalls, arrangeCalls, layoutPasses, cacheHits}`；量之前先 `bringToFront()`）、`created()`（**自启动以来构建过多少行**——虚拟化的核心数字）、`offset()`/`scrollTo(y)`/`scrollRows(n)`、`window()`/`renderedKeys()`/`visibleKeys()`/`viewport()`、`point(key)`（**只在行位于视口内时**返回坐标；`pointMounted()` 是原始位置）、`add/shuffle/swapVisible/click/filter/clear`、`counts()`/`churn(n)`/`state()`；`#demo-state` 逐帧发布 `total/rendered/first/last/created/offset/maxOffset/filter/deleted`。**关键判据**：一行一行地滚，`created` 每步正好 +1（不是重建）；窗口内交换两行 `created` 不增长；overscan 行（挂载但在视口外）点了没反应；`churn(20)` 计数回基线。矩阵与修掉的文档型缺陷（V26）见 [`ACCEPTANCE-list.md`](./docs/ACCEPTANCE-list.md)。
- **滚动与焦点**（`direction` 轴规则见 §8.49：**键自己带轴**，`direction: 'both'` 的口按键盘走各自的方向，单轴口覆盖按键；**拖动归属**见 §8.53：控件在自己的 `pointerdown` 用 `claimPointerDrag()` 认领，`ScrollView` 在越过阈值时才用 `pointerDragOwner()` 问一句，有人认领就让出——文本域拖选与页面滚动因此不再打架）：`#/scroll`（`apps/examples/src/scenes/scroll.ts`）是 `ScrollView` 的常驻验收场：220 行虚拟化列表 + 横向 chip 条（**30 个可聚焦 `Button`**，`focusOrder: -1`）+ **口套口**（外层 `scroll.nested` 带 pinch zoom，里面第 30 行之后是内层 `scroll.inner` 与 12 个可聚焦按钮，两级都要滚）。`window.scrollDemo` 提供 `offsets()`（含 `inner`/`innerMax`）/`visibleRowKey()`/`owners()`/`zoom()`/`setZoom()`/`viewports()`（含 `inner`）/**`watchScroll(port)`/`scrollEvents(port)`（第 85 轮：数某个口的 `'scroll'` 事件，用来验「每次偏移变化都上报了」）**/`owners(port)`（第 86 轮起可查任意口）/`viewports().bounce`（橡皮筋口 `scroll.bounce`：`bounce: true`、`maxOffset=164`）；`#demo-state` 逐帧发布 `v.*`/`h.*`/`nested.*`/`inner.*`/`chip`、**焦点几何**（`focus.x/y/w/h` 是 stage 矩形，`focus.wx/wy/ww/wh` 是走变换矩阵的**世界**矩形——缩放过的口里只有后者是屏幕位置）以及四个口的可见带。**核心判据**：① 拖动归属空闲时必须是 `none`（V24）；② **整个 `Tab`/`Shift+Tab`/`D-Pad` 走查里焦点从不落在可见带外面**，且每步偏移只前进一行（V33）；③ 口滚到尽头后方向键要能**离开**口（V35）；④ 在 chip 条上拖动 300px 不激活任何 chip（`chip=none`）；⑤ **口套口**时两级都动、被聚焦控件同时落在两层的带内（缩放 1 与 1.6 各一遍）；⑥ 拖拽与滚轮一样**用完就传给外层**（V37）。矩阵见 [`ACCEPTANCE-scroll.md`](./docs/ACCEPTANCE-scroll.md)。
- **`UIScene` 基类**：`#/uiscene`（`apps/examples/src/scenes/uiscene.ts`）是"场景就是那一页"的常驻验收场：三个结构不同的视图（A/B/三根）、真实点击换页、`onBack()` 的两个分支、以及 20 次整页替换的泄漏计数。`window.uiscene` 提供 `show(name)`/`swap(n)`/`back()`/`setHook(on)`/`dialog()`/`counts()`/`state()`；`#demo-state` 逐帧发布 `view`/`swaps`/`prev.destroyed`/`roots`/`built`/`container`/`hookBacks`/`appBacks`/`counts.*`/`pt.*`/`st.*`。**核心判据**：① `content()` 自动建树并挂载（`roots=1`、`container=Panel`）；② 换页后 `prev.destroyed=1` 且 `swap(20)` 前后每一项计数相同（不泄漏）；③ `onBack()` 返回 `true` 只加 `hookBacks`、返回 `false` 才加 `appBacks`，有对话框时**两个都不加**；④ 多根视图 `roots=3`/`container=BoxWidget`。矩阵见 [`ACCEPTANCE-uiscene.md`](./docs/ACCEPTANCE-uiscene.md)。
- **插件选项**：`#/config`（`apps/examples/src/scenes/config.ts`）是 `MVVMPlugin.configure`（游戏级默认值，创建游戏前）与 `mvvm.configure`（运行期补丁）的常驻验收场。`window.config` 提供 `patch(options)`/`defaults()`/`motion()`/`cameraColor()`/`liveAttrs()`/`theme(name)`/`focus(name)`/`focusName()`/`router()`/`point(name)`/`state()`；`#demo-state` 另发布 `motion.enter`/`motion.exit`/`motion.reduced`（`transitionFor()` 的实时解析结果，**子选项包深合并的门禁**：只补 `exit` 不能清掉 `enter`）。**关键判据**：`MVVMPlugin.configure` 设的默认值真的到达场景（`aria-live="assertive"`）、`configure({ navigation: false })` 立刻停掉 Tab 但鼠标照常、子选项包一层深合并（改 `focus` 不会清掉 `input`）、`themeBackground: false` 后切主题相机颜色不再跟随。**安全区**：`window.config.safeArea()` 返回 `{ enabled, device, reserved, padding }`——`device.*` 是浏览器报的 inset（CSS 像素）、`safeArea.*` 是根实际预留的（去过黑边重叠并换算到设计像素），`#demo-state` 逐帧发布这两套加 `root.padding.*`，`window.config.resize()` 强制重读（inset 只在 resize 时重读）——这一条只能逐设备验，矩阵见 [`ACCEPTANCE-mobile.md`](./docs/ACCEPTANCE-mobile.md)。矩阵见 [`ACCEPTANCE-config.md`](./docs/ACCEPTANCE-config.md)。
- **无障碍镜像**：`#/a11y`（`apps/examples/src/scenes/a11y.ts`）是 `A11yBridge` 的常驻验收场（普通/开关/禁用按钮、滑杆、必填字段、多行字段、可点击卡片、滚动区 + 区内 6 个按钮，以及一个**不该**被镜像的 `Label`）。`window.a11y` 提供 `nodes()`/`node(name)`/`names()`（**逐属性读真实 DOM**）、`live()`/`liveAttributes()`/`announce(text)`、`focus(name)`/`focusName()`/`focusables()`、`sync()`/`refresh()`/`enabled(on)`、`setVolume(v)`/`validate()`、`counts()`/`state()`。**断言方式**：全部走 `querySelectorAll('[data-mvvm-a11y]')` 与节点属性（不是控件树）——只有这样才能抓到"对象里对、DOM 里错"；**而"读屏软件到底看到什么"只能看浏览器算出来的树**（CDP `Accessibility.getFullAXTree`），所以 `visual-check` 有两张表：`AX_EXPECTATIONS`（控制节点**恰好**这么多、每条恰好一次）与第 103 轮新增的 `AX_STRUCTURE_EXPECTATIONS`（**包含关系**：`region:按钮区域` 里 6 个 button、`group:字段区域` 里 2 个 textbox、`modal` 的 `dialog` 里 2 个 button——镜像 DOM 与控件树同构，文本框的 `<input>` 靠 `aria-owns` 挂进它该属于的节点）。矩阵与三个设计缺口（V29 禁用控件缺席、V30 无 `label` 选项、V74 容器的 `label` 被静默丢弃）见 [`ACCEPTANCE-a11y.md`](./docs/ACCEPTANCE-a11y.md) §11。
- **虚拟键盘（手柄文本输入）**：`#/keyboard`（`apps/examples/src/scenes/keyboard.ts`）是 `VirtualKeyboard` 的常驻验收场：一个 `TextField` + 文字/数字两种键盘 + 清空/换键盘按钮，逐帧发布 `kb.value`/`kb.length`/`kb.caret`/`kb.page`/`kb.kind`/`kb.upper`/**`kb.caps`**/`kb.upperFirst`/`kb.submits`/`kb.focusables` 与每个键的 `pt.kb.<id>`/`st.kb.<id>`；`window.keyboard` 提供 `type(text)`/`press(id)`/`keyFor(char)`/`point(idOrName)`（键 id **或**控件名）/`keys()`/`appearance()`/`value()`/`focus(name)`/`submits()`/`setKind(kind)`/`kind()`/`counts()`/`churn(n)`/**`swapChurn(n)`（async，翻 `kind` ref n 次、每轮等一帧再采样）**，以及第 101 轮的**对话框一族**：`openDialog()`/`closeDialog()`/`dialogState()`（含 `focusables`，用来验"焦点陷阱里只有对话框自己的控件"）/`dialogType(text)`/`dialogPress(id)`/`dialogPoint(idOrName)`/**`dialogChurn(n)`（async，每轮 `await settle()` —— 关闭后的图层要等出场动画才销毁，等两帧会读到"五个还在淡出的图层"）**；`#demo-state` 逐帧发布 `dlg.open`/`dlg.value`/`dlg.length`/`dlg.caret`/`dlg.kind`/`dlg.submits`（销毁后值类读数发 `gone`）。**核心判据**：① D-Pad 能在键间走查、`A` 就是打字（`value` 随之变化）、`Enter` 键提交、`⌫` 删除；② `⇧` 一次只大写**下一个**字符（`upper=on`→打一个字母→`off`）、连按两次才锁定（`caps=on` 且打字后仍是 `on`）——只读 `kb.upper` 分不出这两者，所以两个探针都在；③ 鼠标与触摸点击同样算按键（`st.kb.<id>` 走 `normal → hover → pressed → focused`）；④ `churn(20)`/`swapChurn(20)` 前后 `counts.*` 与 `displayList` 完全相同（换键盘是"加一棵子树 + 销毁另一棵"，V27 的形状）；⑤ 该场景在 `scripts/visual-check.mjs` 里同时有像素（`kb.keyboard` 面板底色、`kb.key.enter` 主键底色，明暗两套）与 **`AX_EXPECTATIONS`**（37 个控制节点逐条断言"角色 + 名字"）。矩阵见 [`ACCEPTANCE-keyboard.md`](./ACCEPTANCE-keyboard.md)（第 101 轮新增 §9：**对话框里的键盘** —— 手柄 `→`/`↓`/`A`、真鼠标、真触摸、屏幕键盘 `Enter` 提交、`Esc` 关闭后焦点回到触发按钮、`dialogChurn(5)` 计数回基线；同轮发现并修掉 V72，见 §8.62）。**换键集是数据槽，不是重建**（第 82 轮）：`kind` 接受字面量/`ref`/getter，键盘**自己**重建自己的键（`drawKeys()`：`Row` 用一个 `BoxWidget`、键用一个 `Button`，直接在控件里造，不经过 DSL），所以"调用方第二次构造键盘"在 API 上不可表达——第 81 轮 V48 那条"重建时漏传 `onSubmit`"的路径被消掉了。切页（`123`）走同一条重建路径：符号页真的没有 `⇧`、数字键盘真的没有字母（V50：在此之前计划说没有、控件只是重新贴标签）。**重建后必须自己把焦点放回去**，两件事缺一不可：① 复合控件（键盘）自己不可聚焦，`Widget#focusManager` 永远是 `null`，要从它的键上拿（`entry.widget.focusManager`）；② `FocusManager.focus()` 只接受**当前收集集合**里的控件，而那个集合此刻还描述着刚被销毁的旧键——所以先 `manager.refresh?.()` 再 `target.focus()`，否则调用被静默忽略、手柄玩家的焦点直接消失（实测修前四种换键集场景全是 `focus=none`）。`FocusTarget` 因此有可选的 `refresh()` 并从包入口导出。**构建期纪律仍在**（V47）：DSL 里造容器必须 `withUiParent()` 认领子节点，否则子节点挂到外层容器上、自己成为一个空盒子（控件数、可点性全都正常，只有几何看得出来）；在**构建趟之外**造控件（点击回调、`Repeat` 行模板）走 `buildUiSubtree(scene, () => …, '…')`，裸调 DSL 会因 `currentUiScene()` 抛错。
- **选项覆盖页**：`#/options`（`apps/examples/src/scenes/options.ts`）是「只出现在控件源码里、没有任何 demo 走过」的选项的验收场（第 87 轮的键表反查），一卡一族、尽量 A/B：`Repeat.update`（原地刷新 vs 销毁重建，读**画出来的文字**而不是数据源）、`TextArea.submitOnEnter`、`ScrollView.inertia`/`wheelSpeed`、`Grid.autoFlow`/`minRowHeight`（注意 `minRowHeight` **只在 `rows` 固定时生效**）、`Row({ wrap, alignContent })`，以及第 90 轮补的 `ScrollView.direction: 'both'`（两个内容与视口完全相同的口做 A/B：交叉轴的上限、`deltaX`、斜向拖动、`revealDescendant` 的 x 分支、以及它自己的滚动条 —— V60 就在这里）。`window.optionsDemo` 提供 `replaceItems()`/`state()`（含 `updatedPainted`/`plainPainted`/`updCalls`/`updBuilds`/`plainBuilds`）/`rects()`（tile 与 chip 的 stage 矩形，用来断言排布）/`points()`/`scrollInfo()`（口的 `inertiaEnabled`/`wheelSpeed`/`offset`/`dragging`/`coasting`/`velocity`）/`scrollStage(y)`（页面比视口高，手势卡在折叠线以下）/`resetScroll()`/`counts()`，以及第 90 轮为双轴口加的 `bothInfo()`（两轴偏移/上限/内容与视口尺寸/槽位读数/拖拽归属）/`setBoth(x,y)`/`setControl(y)`/`pointOf(name)`/`focus(name)`/`watchScroll(name)`/`scrollEvents(name)`/`prepare()`（async：把卡滚进带 + 把两个口的 rect 写进 `#status`）。矩阵见 [`ACCEPTANCE-options.md`](./docs/ACCEPTANCE-options.md) §7–§9（§9 是拖动选择：`selection()`/`selectionRects()` 两个探针 + 真鼠标/真触摸矩阵）。
- **生命周期泄漏门禁**：`#/lifecycle`（`apps/examples/src/scenes/lifecycle.ts`）建一页全控件界面并暴露 `window.lifecycle.churn(n)`：重启场景 n 次、每轮采样 9 项计数（`themeListeners`/`displayList`/`sceneObjects`/`focusables`/`pointerTargets`/`textures`/`tweens`/`timers`/`widgets`/**`frameListeners`**）。验收断言：每项只有一个取值 + `pages()` 只剩当前页 + 重启后 `pt.click` 能点、`pt.name` 能输入。**`frameListeners` 是第 97 轮加的**（`scene.events.listenerCount('preupdate'|'update'|'postupdate')` 之和）：逐帧钩子（`ScrollView` 的滑行、`TextInputBase` 的拖动自动滚动）泄露时其余八项**全都看不见**——阳性对照（注释掉 `off(POST_UPDATE)`）只有它每轮 +2，见 [`ACCEPTANCE-lifecycle.md`](./docs/ACCEPTANCE-lifecycle.md) §5.5。改动插件生命周期、控件销毁、主题订阅**或任何逐帧注册**后必须跑一次。
- **状态槽位（第 84 / 98 / 99 轮）**：`disabled`（`TextField`/`TextArea`/`Slider`/`Button`）、`error`（字段；收文案或 `null`）、`variant`（`Button`/`Panel`）、**`readOnly`（字段）、`maxLines`/`ellipsis`（`Text`）、`texture`/`frame`（`Image`）、`min`/`max`（`Slider`，第 99 轮）** 与 `value` 一样是数据槽 —— 常量 / `ref` / getter 都行，所以「能不能编辑 / 是不是错的 / 这块面什么颜色」不必绕到命令式 `bindEnabled`/`bindError`；`Panel` 因此补了 `setVariant()`（连带按新变体重算默认描边、丢弃皮肤缓存）。`#/compose` 的第 11 个分区 **State slots** 是常驻验收（`window.compose.setState({ locked, error, variant })` 与 `slots()`，逐帧 `state.*` 与 `st.state.*`）；它同时是 `scripts/visual-check.mjs` 的像素门禁：`SCENE_SETUP` 在**运行时**改**四个**槽（`{ variant: "danger", altTexture: true, frozen: true, rangeMax: 200, altFrame: true }`）并断言 `state.panel` `#f85149`/`#cf222e`、`state.tex` 与 `state.frame`/`state.frameAlt` 四个**字面量**采样（贴图与帧色在明暗两半必须相同；`state.frameAlt` 由 `altFrame` 切到 `green`，而同一图集、`frame: 'red'` 字面量的 `state.frame` 必须不动 —— 这就是 `frame` 槽位的 A/B）、`state.frozen` `#1f2630`/`#eef1f4`、以及 `state.rangeA`/`state.rangeB` 这一对**同值不同量程**的滑块（宽度 30% 处：量程 100 的那只读到 `primary` 填充、量程 200 的那只读到 `border` 轨道——「改量程忘了重画」会让两者变成同一个颜色，V67） —— 三个控件分别以 `surface` / 贴图 A / 可编辑构造，所以那些颜色只可能来自真正的重绘（V38 家族）。矩阵与阳性对照（注释掉贴图槽 → `MISMATCH state.tex`）见 [`ACCEPTANCE-compose-dsl.md`](./docs/ACCEPTANCE-compose-dsl.md) §3.2/§3.2.1。
- **结构性切换用 `Branch()`，不是 `visible`**：`visible: () => …` 保留节点、只退出布局流（适合"这个提示出现/消失"）；两个分支要换成**完全不同的树**时用 `Branch(select, { key: () => {…} })`——它按 key 建一个分支，切 key 时销毁旧分支（控件、绑定、订阅、文字纹理全走 `Widget.destroy()` 那条路），入口规则与页面一致（0 根报错、多根警告后包一层容器）。未知 key 会清空当前分支并打一条指名警告，**不会崩**（查询走 `hasOwnProperty`，`constructor`/`toString`/`__proto__` 不会被误当分支，单测 `packages/widgets/test/branch-plan.test.ts`）。状态要放在场景/ViewModel 上，因为分支闭包会随分支一起消失；`#/compose` 的 `Flow` 分区有四个按钮的完整演示与 `branch.*` 读数，矩阵见 [`ACCEPTANCE-compose-dsl.md`](./docs/ACCEPTANCE-compose-dsl.md) §3.1。
- **坐标探针：`reportControl` 是"创建时采样一次"**，只适合布局不会移动的页面；**布局会随使用变化的页面必须逐帧发布 `pt.*`**（`#/states`、`#/compose`、`#/hud`、`#/bindings` 都这么做）。否则探针会指向被挤到别处的控件，验收时表现为"点了没反应"甚至误判为框架缺陷（第 58 轮在 `#/bindings` 上真实发生过）。
- **示例场景约定**：在 `apps/examples/src/scenes/<name>.ts` 导出 `Phaser.Scene` 子类 → 注册进 `apps/examples/src/main.ts` 的 `SCENES`（hash 即场景名）。用 `status.ts` 的 `setStatus`/`appendStatus`/`reportWidget`/`reportCanvas` 输出可断言的几何；`#status`、`#demo-state` 在 CSS 里 `display: none`（内容供机器读，不画到画布上）。注意 `#demo-state` 是**空格分隔**的 `key=value`：值里含空格时（自由文本、输入框内容）必须走场景自己的 API（`window.<scene>.state()`）读取，不要在 DOM 文本上按空格切分。**已销毁的控件要发布 `st.<key>=gone`**（`#/modal`/`#/pages` 这么做），否则对话框关掉后 `st.*` 会停在最后一帧，读起来像"卡住了"。**`pt.*` 同理**：`#demo-state` 是**一整个页面共享**的一行（`demo.ts` 的 Map 是模块级的，跨场景保留），某个控件被销毁后它的 `pt.<key>` 会一直留在最后坐标上，验收脚本照着点就会点到别的东西（第 69 轮在 `#/compose` 的 `Branch` 演示里踩到：切走分支后 `pt.branch.a.inner` 还在，于是补了 `gone`）。`?capture=1` 会开启 `preserveDrawingBuffer`，否则截图读不到 WebGL 帧。
- **性能预算**（PR 评审依据，PLAN §8）：1000 节点全量 `measure+arrange` < 1.5 ms（实测 0.06 ms）；无变化帧布局耗时 = 0（实测 0.024 ms、零测量）；布局约束缓存命中率 > 95 %（实测 95.6 %）；单节点编辑只重测该子树（实测 922 个节点里只重测 1 个）；排布热路径零新增对象/闭包（对象池长度稳定）；gzip 体积 `core`+`layout` < 25 KB、`phaser`+`widgets` < 45 KB（按 min+gzip，实测 18.3 / 14.7 KB）；文本度量缓存已实现（`packages/widgets/src/text-metrics.ts`，稳态命中率 100%）。怎么跑见 §8。
- **CI**（`.github/workflows/ci.yml`）：Prettier 检查 → 逐包 `typecheck` → 逐包 `test` → 逐包 `build` → 示例构建，PR 必须全绿。`perf.test.ts` 随逐包 `test` 一起跑；`pnpm size` 尚未接进 CI（需要先 build），本地提交前跑一次即可。

## 7. 提交前自查清单

1. `pnpm format`（或只格式化改动文件），确保 `pnpm exec prettier --check .` 通过。
2. `pnpm --filter @phaser-mvvm/<受影响包> run typecheck` 与 `run test`。
3. 改动涉及包 `src/` 且被示例消费时：`pnpm run build:examples`；涉及控件外观/布局几何/输入交互时：`node scripts/visual-check.mjs`（等价于先 build 再验收），或用 Playwright MCP 打开 `http://localhost:5173/#/showcase`｜`#/compose` 读 `#status`/`#demo-state` 做几何与状态断言（`pt.<key>=@x,y` 是每个可点击控件的页面坐标）。
4. 触及硬约束 §4 的任一条 → 先补/改 ADR，再写代码。
5. 汇报时给出**实际运行过的命令与结果**，区分「已验证」与「未验证」。

## 8. 已知易踩的坑

> 每条**完整说明**（现象 → 根因 → 修法 → 实测数字）在 [`docs/PITFALLS.md`](./docs/PITFALLS.md)，编号一一对应。下面按主题列出一行索引：开工前扫一眼；要做判断时打开那一条读完整说明，别只看标题就动手。

**选项 / DSL / 值语义**：[§8.4](./docs/PITFALLS.md) 选项审计：拼错的选项会被指名；[§8.19](./docs/PITFALLS.md) `UIScene.content()` 与 `ui()`/`render()` 的 lambda 是同一种东西；[§8.33](./docs/PITFALLS.md) 字段的 `setValue()` 与"用户编辑"是两件事；[§8.34](./docs/PITFALLS.md) 可访问名有两个来源，别让它们打架；[§8.38](./docs/PITFALLS.md) 滚动位置是状态，而且只有一个写入口；[§8.46](./docs/PITFALLS.md) 编辑权限判在唯一漏斗上，别判在每个调用点（`Ctrl+X`/`Ctrl+V` 曾经绕过 `readOnly`）。

**输入 · 焦点 · 手势**：[§8.53](./docs/PITFALLS.md) 拖动归属：控件先认领，滚动口再决定（文本域拖选 vs 页面滚动）；[§8.49](./docs/PITFALLS.md) 双轴口的"轴"由键决定，不由 `direction` 决定；[§8.50](./docs/PITFALLS.md) 自动化输入设备的滚轮增量可能与请求值不同（CDP 会翻倍）；[§8.16](./docs/PITFALLS.md) 焦点也要"滚进视野"，而且这是框架的责任；[§8.17](./docs/PITFALLS.md) 容器不许抢自己里面控件的方向；[§8.18](./docs/PITFALLS.md) "认领方向"必须是诚实的；[§8.27](./docs/PITFALLS.md) 手柄验收怎么做；[§8.40](./docs/PITFALLS.md) 触摸验收怎么做。

**布局 · 渲染 · 主题**：[§8.3](./docs/PITFALLS.md) 布局缓存按 (约束, 百分比基准, revision) 命中；[§8.48](./docs/PITFALLS.md) 构建趟里 `reportWidget()` 读到的是 `0×0`（读数要等一帧）；[§8.13](./docs/PITFALLS.md) Phaser 的 `Container#remove` 会把子节点交回场景显示列表；[§8.14](./docs/PITFALLS.md) 裁剪容器也裁剪命中；[§8.29](./docs/PITFALLS.md) 示例有两种缩放模式；[§8.30](./docs/PITFALLS.md) 画布可能被缩放：一切"设计坐标 ↔ 页面坐标"的换算都要过 `displayScale`；[§8.31](./docs/PITFALLS.md) 安全区（刘海 / 手势条）默认就开着，但需要页面配合。

**无障碍**：[§8.35](./docs/PITFALLS.md) 无障碍镜像跟着"可交互集合"走；[§8.36](./docs/PITFALLS.md) `[data-mvvm-a11y]` 的属性 ≠ 屏幕阅读器看到的东西；[§8.37](./docs/PITFALLS.md) 控件状态变化要主动告诉无障碍桥。

**生命周期 · 页面 · 动效**：[§8.51](./docs/PITFALLS.md) `duration: 0` 的语义是"应用终态"，别在调用方把它过滤掉；[§8.7](./docs/PITFALLS.md) 起了后台服务就一定要确认收尾；[§8.10](./docs/PITFALLS.md) `back`（`Esc` / 手柄 B）的归属是逐层路由的；[§8.20](./docs/PITFALLS.md) 页面转场同样是「语义立即、画面延后」，而且邻居页是「画着但不可点」；[§8.21](./docs/PITFALLS.md) 动效时长是主题令牌，不是常量；[§8.22](./docs/PITFALLS.md) 动效把"关闭"拆成了两件事，别的门禁要跟着改；[§8.23](./docs/PITFALLS.md) `UIScene` 的 `setContent()` 是"销毁整页 + 重建"；[§8.24](./docs/PITFALLS.md) `back` 现在有四层，第 3 层是场景。

**工具 · 环境 · 验收**：[§8.1](./docs/PITFALLS.md) 开发期包入口指向源码；[§8.5](./docs/PITFALLS.md) 调试日志；[§8.6](./docs/PITFALLS.md) 性能/体积预算怎么跑；[§8.8](./docs/PITFALLS.md) 本机没有 `timeout` 命令；[§8.9](./docs/PITFALLS.md) Playwright MCP 的页面默认 rAF 被节流到 1 fps（必须在断言前 `page.bringToFront()`）；[§8.11](./docs/PITFALLS.md) Phaser 的键盘管理器会丢弃 `defaultPrevented` 的按键；[§8.12](./docs/PITFALLS.md) 虚拟化列表里的"可见"有两层；[§8.15](./docs/PITFALLS.md) 拖动归属必须释放；[§8.25](./docs/PITFALLS.md) 嵌套口的"用完传出去"对所有手势都成立；[§8.26](./docs/PITFALLS.md) 示例应用按哈希选场景，改哈希会整页重载；[§8.28](./docs/PITFALLS.md) 插件选项的正确写法；[§8.41](./docs/PITFALLS.md) 文档数字会滞后；[§8.42](./docs/PITFALLS.md) 不要引入浏览器测试框架；[§8.47](./docs/PITFALLS.md) 一次性几何探针会在布局变化后撒谎（先怀疑探针、再怀疑框架）。

**可访问性 / 层级**：[§8.62](./docs/PITFALLS.md) 模态挡住了画布，但挡不住无障碍树（V72：被盖住的页面必须 `aria-hidden`，含它自带的 DOM 元素；聚焦中的控件例外）；[§8.63](./docs/PITFALLS.md) "被盖住"有两种（模态 vs 页面栈），判据不同：模态是"层以外一律隐藏"，页面是"只隐藏 `active === false` 的页"（V73）；[§8.65](./docs/PITFALLS.md) 指南里的选项键必须真的被代码接受（门禁 `check-doc-options.mjs`；四个"归属"坑：DSL 槽位、标题栈、`import { type X }`、带标注的 `*_KEYS` 数组）；[§8.64](./docs/PITFALLS.md) 镜像必须是**树**（嵌套后 `aria-hidden` 会继承 → 聚焦控件的祖先链也不能隐藏；自带 DOM 元素的控件靠 `aria-owns` 进树；容器 `label` 曾"审计接受、类型拒绝、构造时静默丢掉" V74）。

**重绘 · 数据流**：[§8.61](./docs/PITFALLS.md) 公开读数要报告解析后的值，不是请求的值（`currentFrame` 会撒谎 V71）；[§8.59](./docs/PITFALLS.md) 重画缓存键必须包含画的时候读到的每一个输入（滑块量程 V67）；[§8.60](./docs/PITFALLS.md) 控件自己改了值就要说出来（静默写值会让双向绑定永久不一致 —— 滑杆 V69、开关 V70）。

**韧性/边界**：[§8.58](./docs/PITFALLS.md) 被截断的文本必须"说出来"，而"能被撑宽"的宽度什么也证明不了；[§8.57](./docs/PITFALLS.md) "按住不动"是逐帧的事，`pointermove` 里做不到（拖到边缘自动滚动）；[§8.56](./docs/PITFALLS.md) 事件名承诺"变化"，就别在每次重绘时发；[§8.54](./docs/PITFALLS.md) 点击计数是"时间 + 距离"窗口，探针别连点；[§8.43](./docs/PITFALLS.md) 橡皮筋：越界位移不能被布局钳掉；[§8.44](./docs/PITFALLS.md) 数据槽位会「每帧写回同一个值」，这类写入必须是空操作。

**其它**：[§8.55](./docs/PITFALLS.md) 公开 API 里"没人用过的那一个名字"（三份语料一起查）；[§8.52](./docs/PITFALLS.md) 指南与示例是被抄的代码，改完 API 要一起扫；[§8.2](./docs/PITFALLS.md) `dist/`、`.tmp/`、`coverage/`、`test-results/` 都是生成物；[§8.32](./docs/PITFALLS.md) `Rect` 收的是颜色字面量，不是令牌；[§8.39](./docs/PITFALLS.md) "这个方向归谁"只写一遍。

---

## 9. 变更范围纪律

- 只改与任务相关的文件；当前工作区可能已有他人未提交的在制品（`git status` 先看），不要顺手格式化或回滚它们。
- 不要为了「让检查通过」而降低断言强度、跳过测试或删除门禁脚本；发现真实缺陷就修根因，并在提交信息/汇报中说明。
