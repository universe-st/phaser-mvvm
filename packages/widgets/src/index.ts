/**
 * `@phaser-mvvm/widgets` — the widget library (Panel, Label, TextField, TextArea, Button, Image,
 * Spacer, Divider, ScrollView, Repeat, Modal).
 *
 * Scheduled for M4–M7 in `docs/PLAN.md`; the M0–M2 milestones only ship the layout engine, the
 * reactive core and the Phaser adapter (`Widget`, `UIRoot`, the box/grid/stack/absolute widgets and
 * the minimal `RectWidget` / `LabelWidget` probes, which live in `@phaser-mvvm/phaser` for now).
 */

export {};
